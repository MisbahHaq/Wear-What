@model Shop.Models.Product

@{
    ViewData["Title"] = Model.Name;
    var currentUserId = User.FindFirst(System.Security.Claims.ClaimTypes.NameIdentifier)?.Value;
}

<h1>@Model.Name</h1>

@if (Model.Category != null)
{
    <p class="detail-meta"><strong>Category:</strong> @Model.Category.Name</p>
}
@if (!string.IsNullOrEmpty(Model.Niche))
{
    <p class="detail-meta"><strong>Niche:</strong> @Model.Niche</p>
}

<div class="row">
    <div class="col-md-6">
        @if (!string.IsNullOrEmpty(Model.ImageUrl1))
        {
            <img src="@Model.ImageUrl1" class="detail-image img-fluid" alt="Product Image 1" />
        }
        @if (!string.IsNullOrEmpty(Model.ImageUrl2))
        {
            <img src="@Model.ImageUrl2" class="detail-image img-fluid" alt="Product Image 2" />
        }
        @if (!string.IsNullOrEmpty(Model.ImageUrl3))
        {
            <img src="@Model.ImageUrl3" class="detail-image img-fluid" alt="Product Image 3" />
        }
        @if (!string.IsNullOrEmpty(Model.ImageUrl4))
        {
            <img src="@Model.ImageUrl4" class="detail-image img-fluid" alt="Product Image 4" />
        }
        @if (!string.IsNullOrEmpty(Model.ImageUrl5))
        {
            <img src="@Model.ImageUrl5" class="detail-image img-fluid" alt="Product Image 5" />
        }
    </div>
    <div class="col-md-6">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Description</h5>
                <p class="card-text">@Model.Description</p>
                <h5 class="card-title">Price</h5>
                <p class="detail-price">$@Model.Price.ToString("0.00")</p>
                <h5 class="card-title">Shop</h5>
                <p class="card-text"><a asp-controller="Shops" asp-action="Details" asp-route-id="@Model.ShopId">@Model.Shop?.Name</a></p>
                @if (Model.Shop?.OwnerId == currentUserId)
                {
                    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-outline-secondary">Edit</a>
                    <a asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-outline-danger">Delete</a>
                }
                else if (currentUserId != null)
                {
                    <form asp-controller="Wishlist" asp-action="Add" method="post" class="d-inline mb-2" asp-antiforgery="true">
                        <input type="hidden" name="productId" value="@Model.Id" />
                        <button type="submit" class="btn btn-outline-primary">Add to Wishlist</button>
                    </form>
                    <form asp-controller="Orders" asp-action="Create" method="post" class="mt-2" asp-antiforgery="true">
                        <input type="hidden" name="productId" value="@Model.Id" />
                        <div class="mb-2">
                            <label for="quantity" class="form-label">Quantity</label>
                            <input type="number" name="quantity" id="quantity" class="form-control" value="1" min="1" max="100" style="width: 100px;" />
                        </div>
                        <button type="submit" class="btn btn-primary">Place Order</button>
                    </form>
                }
                <a asp-action="Index" class="btn btn-outline-secondary">Back to Products</a>
            </div>
        </div>
    </div>
</div>

@if (Model.Comments.Any())
{
    <hr />
    <h3>Reviews</h3>
    <div class="list-group">
        @foreach (var comment in Model.Comments!)
        {
            <div class="list-group-item">
                <div class="d-flex justify-content-between">
                    <h5 class="mb-0">@comment.User?.FullName</h5>
                    <span class="text-muted">
                        @for (int i = 1; i <= 5; i++)
                        {
                            if (i <= comment.Rating)
                            {
                                <span class="review-star">&#9733;</span>
                            }
                            else
                            {
                                <span class="review-star">&#9734;</span>
                            }
                        }
                    </span>
                </div>
                <p class="mb-1">@comment.Content</p>
                <small class="text-muted">@comment.CreatedAt.ToString("g")</small>
                @if (comment.UserId == currentUserId)
                {
                    <div class="mt-2">
                        <a asp-controller="Comments" asp-action="Edit" asp-route-id="@comment.Id" class="btn btn-sm btn-outline-primary">Edit</a>
                        <a asp-controller="Comments" asp-action="Delete" asp-route-id="@comment.Id" class="btn btn-sm btn-outline-danger">Delete</a>
                    </div>
                }
            </div>
        }
    </div>
}

@if (currentUserId != null)
{
    <hr />
    <a asp-controller="Comments" asp-action="Create" asp-route-productId="@Model.Id" class="btn btn-primary">Add Review</a>
}

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
